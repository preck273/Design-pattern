/*
package Staff;

import Customer.Customer;
import Order.Order;
import Pancake.*;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class WaiterTest {
    Customer customer = new Customer(1, "James Kay");
    Order order = new Order(customer);
    SweetPancake sweetPancake = new SweetPancake();
    public Waiter waiter = new Waiter(01, "Merry", 50.0, 8);
    @Test
    void calculateCheck() {
        assertEquals(0.0, waiter.calculateCheck(order));
    }
    @Test
    void makePancake() {
        assertEquals(null, waiter.makePancake(sweetPancake));
    }
    @Test
    void calculateSalary() {
        assertEquals(400.0, waiter.calculateSalary());
    }
    @Test
    void getOrder() {
        assertEquals(order, waiter.getOrder(order));
    }
}*/
